PS1="PROMPT> "
ls
touch a
ls -l
echo "Tuesday! " > a
ls -l
# now for comments
rm a
ls -l

true && echo success || echo fail
false && echo success || echo fail

true && echo passed true and
false && echo passed false and

true || echo passed true or
false || echo passed false or

# the case of a subshell
# no prompt gets printed for bash (v4.1.2(1)-release+), so
# imagine one.

bash
ls
pwd
echo "Hello" | grep H
ps
